#include <stdio.h>

int main() {
    int n1;
    printf("Digite um numero inteiro: ");
    scanf("%d", &n1);

    if(n1 % 2 == 0) { //SE O RESTO DA DIVISÃO POR 2 POR 0, ELE EXECUTA O QUE ESTÁ DENTRO
        printf("%d e par", n1);
    }
    else {
        printf("%d e impar", n1);
    }
}// upadte for commit
// update for commit
// update for commit
// update for commit
// update for commit
// update for commit
