#include <stdio.h>

int main() {
    int n, valueByUser;
    printf("Digite um valor: ");
    scanf("%d", &valueByUser);
    for (n = 1; n <= valueByUser; n += 1) {
        printf("%d\n", n);
    }
    return 0;
}// upadte for commit
// update for commit
// update for commit
// update for commit
// update for commit
// update for commit
