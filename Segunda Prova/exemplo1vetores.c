#include <stdio.h>

int main() {
    float notas[10]; //cria um vetor com 10 posições
    //o vetor foi alocado, mas não foi inicializado, ou seja, não tem valor armazenado!!

    notas[0] = 5; //para acessar uma posição desejada do vetor e atribuir um valor a ela (posição 0).
    notas[1] = 9; //para acessar uma posição desejada do vetor e atribuir um valor a ela (posição 1).
    notas[2] = 10; //para acessar uma posição desejada do vetor e atribuir um valor a ela (posição 2).

    return 0;
}