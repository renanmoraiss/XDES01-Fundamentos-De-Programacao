#include <stdio.h>
#include <stdlib.h>

int main () {
    int N1, N2, res;
    printf("Digite um valor: ");
    scanf("%d", &N1);
    printf("Digite um valor: ");
    scanf("%d", &N2);
    res = calculo(N1,N2);
    printf("Resultado: %d\n", res);
    /////
    printf("Digite um valor: ");
    scanf("%d", &N1);
    printf("Digite um valor: ");
    scanf("%d", &N2);
    res = calculo(N1,N2);
    printf("Resultado: %d\n", res);

    return 0;
}

int calculo(int n1, int n2, int res) {
    res = n1 * n2;
    return(res);
}// upadte for commit
// update for commit
// update for commit
// update for commit
// update for commit
// update for commit
