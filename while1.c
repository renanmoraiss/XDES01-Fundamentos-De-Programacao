#include <stdio.h>

int main() {
    int number;
    number = 1;
    while (number <= 18) {
        printf("%d\n", number);
        number += 1; //-> number = number + 1
    }
    return 0;
}

/*
while (condition) {
block of commands;
}
*/// upadte for commit
// update for commit
// update for commit
// update for commit
// update for commit
// update for commit
